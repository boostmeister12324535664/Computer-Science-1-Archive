# Repetition04.py
# This program displays the value of the loop counter  
# which is also called the "Loop Control Variable" (LCV).
# Note that even though the loop repeats 20 times,
# the counter actually counts from 0 to 19.


print()

for k in range(20):
   print(k, end = " ")
   
print()   
